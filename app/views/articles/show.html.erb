<h1><%= @article.title %></h1>
<p><%= @article.body %></p>
<ul>
  <li><%= link_to 'Destroy', article_path(@article), data: { turbo: true ,turbo_method: :delete, turbo_confirm: 'Are you sure?' } %></li>
<!--  <li><%#= button_to  'Destroy', article_path(@article), method: :delete, form: {data: {turbo_confirm: 'Are you sure?'}} %></li>-->
  <li><%= link_to 'Edit', edit_article_path(@article) %></li>
  <li><%= link_to 'Back to home page', articles_path %></li>

  <h2>Comments</h2>
  <%= render @article.comments %>

  <h2>Add a comment</h2>
  <%= render 'comments/form' %>

</ul>